This is the readme_group11.txt to provide guidance on how to install the project of group 11.

## Group Info

Class         : COM3102 - L02
Group no.     : 11
Group members : s186984 CAO Shuo
                s187184 CHAN Ho Lun
                s187208 LENUG Ho Wai

## Guidance

First, in the project folder, install typescript:

```
npm install @types/react typescript
```

Hence, install the required packages:

```
npm install @mui/material @emotion/react @emotion/styled @mui/icons-material @fontsource/roboto @mui/lab @mui/x-data-grid

```

And then run the server:

```
npm run dev
```

The starting page would be [http://localhost:3000/pages/login](http://localhost:3000/pages/login) which is at `pages/api/pages/login.tsx`.

